extends layout

block content
  .dashboard
    h1= title
    p wo hao wu yu
    p JADE Welcome to #{title}
    each item in data
      p data.id = #{item.id}

block style
  link(rel='stylesheet', href='/css/dashborad.css')