extends ../../layout

block style
  link(rel='stylesheet/less', href='/lib/swiper/css/swiper.min.css')
  link(rel='stylesheet/less', href='/css/my.swiper.css')
block content
  include ../../component/navbar.pug
  .container
    .row
      .col-md-3
        include ../component/menu.pug
      .col-md-9
        h2 fade
        p fade效果参数。可选参数：crossFade(3.03启动)。
        p 默认：false。关闭淡出。过滤时，原slide透明度为1(不淡出)，过渡中的slide透明度从0->1(淡入)，其他slide透明度0。
        p 可选值：true。开启淡出。过滤时，原slide透明度从1->0(淡出)，过渡中的slide透明度从0->1(淡入)，其他slide透明度0。
  
        h4 fade参数
        p 启用版本：<span class="label label-info">3.0.0</span>

        h4 效果演示
        .desc
          p 缓慢拖动查看对比
          .swiper-container#e1
            .swiper-wrapper
              .swiper-slide slider1
              .swiper-slide slider2
              .swiper-slide slider3
              .swiper-slide slider4
              .swiper-slide slider5
              .swiper-slide slider6
              .swiper-slide slider7
              .swiper-slide slider8
          p 
            span.label.label-danger false
          .swiper-container#e2
            .swiper-wrapper
              .swiper-slide slider1
              .swiper-slide slider2
              .swiper-slide slider3
              .swiper-slide slider4
              .swiper-slide slider5
              .swiper-slide slider6
              .swiper-slide slider7
              .swiper-slide slider8
          p 
            span.label.label-danger true

block script
  script(src="/lib/swiper/js/swiper.jquery.min.js")
  script(src="/js/my.swiper.js")
  script.
    $(function() {
      var e1 = new Swiper('#e1', {
        effect : 'fade',
        fade: {
          crossFade: false,
        }
      });
      var e2 = new Swiper('#e2', {
        effect : 'fade',
        fade: {
          crossFade: true,
        }
      });
      e1.params.control = e2;
      e2.params.control = e1;
    })
