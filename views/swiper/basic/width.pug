extends ../../layout

block style
  link(rel='stylesheet/less', href='/lib/swiper/css/swiper.min.css')
  link(rel='stylesheet/less', href='/css/my.swiper.css')
block content
  include ../../component/navbar.pug
  .container
    .row
      .col-md-3
        include ../component/menu.pug
      .col-md-9
        h2 width
        p 强制Swiper的宽度，当你的Swiper<span class="txt-danger">在隐藏状态下初始化时才用得上。这个参数会使自适应失效。</span>
        h4 width参数
        p 类型：<span class="label label-info">number</span> 举例：<span class="label label-info">500</span> 启用版本：<span class="label label-info">3.07</span>
        h4 效果演示
        .desc
          .swiper-container.hidden#e1
            .swiper-wrapper
              .swiper-slide slider1
              .swiper-slide slider2
              .swiper-slide slider3
          p
            button.btn.btn-warning#b1(type="button") 显示Swiper

block script
  script(src="/lib/swiper/js/swiper.jquery.min.js")
  script(src="/js/my.swiper.js")
  script.
    $(function() {
      $("#b1").click(function(e) {
        $("#e1").toggleClass('hidden');
      })
      var e1 = new Swiper('#e1', {
        width: 500,
      })
    })