extends ../../layout

block style
  link(rel='stylesheet/less', href='/lib/swiper/css/swiper.min.css')
  link(rel='stylesheet/less', href='/css/my.swiper.css')
block content
  include ../../component/navbar.pug
  .container
    .row
      .col-md-3
        include ../component/menu.pug
      .col-md-9
        h2 new Swiper(swiperContainer,parameters)
        p.load.text-warning 用于初始化一个Swiper，返回初始化后的Swiper实例。
        p swiperContainer:必选，HTML元素或者string类型，Swiper容器的css选择器，例如".swiper-container"。
        p parameters: 可选，参加<strong>配置选项</strong>。
        .desc
          h4 效果演示
          p 效果中用css给slide设置了高度和背景色。
          .swiper-container
            .swiper-wrapper
              .swiper-slide slider1
              .swiper-slide slider2
              .swiper-slide slider3
block script
  script(src="/lib/swiper/js/swiper.jquery.min.js")
  script(src="/js/my.swiper.js")
  script.
    $(function() {
      var mySwiper = new Swiper('.swiper-container', {
        autoplay: 5000,
      })
    })